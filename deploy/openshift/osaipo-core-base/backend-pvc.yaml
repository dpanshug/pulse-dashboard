apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: team-tracker-data
  annotations:
    kubernetes.io/reclaimPolicy: Delete
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
